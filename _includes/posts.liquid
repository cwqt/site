<div class="list">
  {% for post in collections.posts reversed %} {% if post.data.draft == false or
  post.data.draft == null %}
  <li>
    <span>{{ post.data.date | date: "%d.%m.%Y" }}</span>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
  </li>
  {% endif %} {% endfor %}
</div>
