---
layout: page
permalink: store
title: store
---

| {% for item in site.data.store %}
.store-item
	.left
		img(src="{{item.image}}")
	.right
		.header
			h1 {{ item.name }}
			p 
				b {{ item.price }}
		p {{ item.desc }}
		.links
			| {% for link in item.links %}
			p {{ link[0] }}: 
				a(href="{{ link[1] }}") {{ link[1] }}
			| {% endfor %}
| {% endfor %}

hr

h3 How to purchase
ul
	li Decide what you'd like
	li Send me an 
		a(href="https://{{site.title}}/contact") e-mail
		|  with a list of what you want and the delivery location
	li I respond with the final price (including shipping) 
	li Send monies via BTC or Paypal
	li I ship your stuff ;)